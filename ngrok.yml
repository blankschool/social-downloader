version: "2"
# Token lido do env pelo ngrok. Configure: ngrok config add-authtoken $NGROK_AUTH_TOKEN
# NÃO commite este arquivo com token real.
authtoken: ""
tunnels:
  backend:
    proto: http
    addr: 8000
    domain: savedown.ngrok.app
